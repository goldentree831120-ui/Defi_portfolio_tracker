# Defi_portfolio_tracker
A Java/Spring Boot backend that connects directly to Ethereum using Web3j to read ERC-20 balances and tiered-APY staking positions. It periodically caches the data and exposes it through a REST API with a simple dashboard. The full setup has been deployed and verified end-to-end on the Sepolia testnet.
